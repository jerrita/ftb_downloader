name: Learn actions

on: push

jobs:
  pack:
    name: Pull and pack
    runs-on: macOS-latest
    steps:
      - name: Pulling files
        run: |
          # wget https://api.modpacks.ch/public/modpack/5/121/server/mac
          # chmod +x mac
          # ./mac 5
          mkdir -p /workdir
          cd /workdir && touch file.txt
          echo test > file.txt
      - name: Pack files
        run: |
          cd /workdir && cat file.txt
